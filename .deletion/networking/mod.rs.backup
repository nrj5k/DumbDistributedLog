//! Stratified Networking Module
//!
//! Core transport traits and interfaces for Quinn-based networking
//! with separate control and data plane optimizations.

pub mod transport_traits;

// Re-export the core types for convenience
pub use transport_traits::{
    BackpressureLevel, ControlMessage, ControlPlaneTransport, ControlResponse,
    DataPayload, DataPlaneTransport, DataReceiver, DataSender, FactoryError,
    HealthStatus, NodeId, StreamId, Transport, TransportError, TransportFactory, TransportStats,
};
